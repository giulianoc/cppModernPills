#include <iostream>

using namespace std;

int main()
{
    // Container adaptors (there are 3)
    //  - are implemented with the fundamental container classes
    //  - provides a restricted interface to meet special needs

    // 1. stack: LIFO, push(), pop(), top()
    // 2. queue: FIFO, push(), pop(), front(), back()
    // 3. priority queue: first item always has the greatest priority
    //      push(), pop(), top()

    return 0;
}
