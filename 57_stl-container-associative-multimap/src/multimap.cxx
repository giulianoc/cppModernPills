#include <iostream>
#include <map>

using namespace std;

int main()
{
    // multimap is a map that allows duplicated keys. So insertion into a multimap is always successful
    // Properties of a set are still valid for a multimap

    multimap<char, int> mymap;

    return 0;
}
