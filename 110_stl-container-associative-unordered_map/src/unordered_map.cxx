#include <iostream>
#include <unordered_map>

using namespace std;

int main()
{
    // unordered_map is a unordered_set of pairs of key-value
    // unordered_map does not have elements with duplicate keys
    // it has the [] operator, important for the associative array (see dedicated pill)
    // Properties of a unordered_set are still valid for a unordered_map

    unordered_map<char, int> mymap;



    return 0;
}
